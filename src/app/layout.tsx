import "./globals.css";
import { Raleway } from "next/font/google";

const inter = Raleway({ subsets: ["latin"] });

export const metadata = {
  title: "NCoffee",
  description: "Generated by Chris Depta.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="pl">
      <body className={inter.className}>
        <div className="overflow-x-hidden bg-white text-black min-h-screen w-screen flex flex-col items-center justify-start ">
          {children}
        </div>
      </body>
    </html>
  );
}
